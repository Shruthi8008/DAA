#include<stdio.h>
int main()
{
    int arr[10]={2,3,4,6,9};
    int n=sizeof(arr)/sizeof(arr[0]);
    for(int i=0;i<=n;i++)
        {
            if(arr[i]>arr[i+1])
                printf("%d is largest element ",arr[i]);
        }
    return 0;
}
